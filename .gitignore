# Secrets — never commit
.env
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# OS
.DS_Store
Thumbs.db

# Raw data and results are intentionally NOT ignored: preserving raw model
# outputs and the run manifests is a reproducibility requirement of this study.
# Only the synthetic mock-provider pilot is excluded, since it contains no real
# model outputs and would be mistaken for data.
data/raw/pilot_mock/
